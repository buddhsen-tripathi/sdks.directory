# Submissions

Use these templates when proposing catalog additions via PR.

| File | For |
|------|-----|
| [`sdk.example.json`](sdk.example.json) | New or updated SDK entry (merge into `src/data/sdks.ts`) |

Optional fields like `skillPaths` are for review; the TypeScript seed may not store them yet.
