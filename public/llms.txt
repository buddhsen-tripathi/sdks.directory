# sdks.directory

> Official SDKs, agent plugins, MCP servers, and skills. Skill responses include full SKILL.md content.

## For agents

Start here:

- Discovery JSON: `/api`
- OpenAPI: `/openapi.json`
- Unified search: `/api/search?q=`
- Catalog MCP: `/api/mcp`
- MCP card: `/.well-known/mcp.json`
- Health: `/api/health`
- Coverage: `/api/coverage`

### Search everything

```
GET /api/search?q=stripe&limit=20
```

Returns ranked SDKs, plugins, MCPs, and skills in one payload.

### Fetch a single skill (preferred)

```
GET /api/skills/stripe/stripe-best-practices
```

Response fields:

- `name`, `sdk`, `key` — identifiers
- `content` — full SKILL.md (YAML frontmatter + body)
- `url`, `install` — upstream source / install hint
- `hasContent` — whether body is present

Raw markdown:

```
GET /api/skills/stripe/stripe-best-practices.md
Accept: text/markdown
```

### List / search skills

```
GET /api/skills?q=redis&withContent=1
GET /api/skills?sdk=neon&include=body
```

List endpoints omit bodies by default (smaller payloads). Pass `include=body` when you need them.

### SDKs / plugins / MCPs

```
GET /api/sdks?withSkills=1&q=auth
GET /api/sdks/stripe?view=agent
GET /api/plugins?q=stripe&platform=claude
GET /api/mcps?category=database
GET /api/mcps/github
```

`view=agent` on SDK detail includes skill bodies (one-shot happy path). MCP entries include derived `transport`, `auth`, and `remoteUrl` when known.

### Catalog MCP tools

POST JSON-RPC to `/api/mcp`:

- `search_catalog`
- `get_sdk`
- `get_skill`
- `get_plugin`
- `get_mcp`

## Human site

- SDKs: `/`, `/browse`, `/sdk/{slug}`
- Plugins: `/plugins`, `/plugin/{slug}`
- MCPs: `/mcps`, `/mcp/{slug}`

## Notes

Skill bodies are snapshotted from upstream (skills.sh / GitHub). Use `/api/skills/{sdk}/{name}` for the SKILL.md text; `url` is the upstream source. Plugins and MCPs are curated seeds — not a raw registry dump.
